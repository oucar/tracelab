# Copy to backend/.env and fill in.
OPENAI_API_KEY=sk-...

# 1 = mini models everywhere (cheap day-to-day default). 0 = per-role models.
CHEAP_MODE=1

# Optional overrides
# ANALYST_MODEL=gpt-4o-mini
# COMPOSER_MODEL=gpt-4o-mini
# SANDBOX_TIMEOUT_SECONDS=20
